import { PronunciationOfWordPipe } from './pronunciation-of-word.pipe';

describe('PronunciationOfWordPipe', () => {
  it('create an instance', () => {
    const pipe = new PronunciationOfWordPipe();
    expect(pipe).toBeTruthy();
  });
});
